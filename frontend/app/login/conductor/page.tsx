import type { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Conductor Login — EzyBus',
    description: 'Sign in to your EzyBus conductor account',
};

export { default } from './_page';
