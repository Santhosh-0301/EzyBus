import type { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Admin Login — EzyBus',
    description: 'Sign in to your EzyBus admin account',
};

export { default } from './_page';
