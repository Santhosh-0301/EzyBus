import type { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Commuter Login — EzyBus',
    description: 'Sign in to your EzyBus commuter account',
};

export { default } from './_page';
